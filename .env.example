# Copiar a .env (ya gitignored) y completar con valores reales.
# .env se carga via `node --env-file=.env <script>`.

# Resend API key — sacar de https://resend.com/api-keys
# Necesaria para scripts/send-beta-invite.mjs y otros envios transaccionales.
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
