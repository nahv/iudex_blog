name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Inject env.js from secrets
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
        run: |
          # Usamos `window.ENV = ...` (no `const ENV = ...`) para que el
          # objeto sea accesible desde ES modules, no solo desde scripts
          # clasicos. main.js (clasico) lo sigue leyendo via `typeof ENV`
          # — el scope chain global resuelve window.ENV. supabase.js
          # (modulo) lo lee como window.ENV explicitamente.
          printf 'window.ENV = {\n  supabase: {\n    url: "%s",\n    anonKey: "%s",\n  },\n};\n' \
            "$SUPABASE_URL" "$SUPABASE_ANON_KEY" \
            > public/js/env.js

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
